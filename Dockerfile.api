FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY server ./server

RUN mkdir -p /data

ENV NODE_ENV=production
ENV PORT=3001
ENV DATABASE_PATH=/data/devops_ai.db

EXPOSE 3001

CMD ["node", "server/index.js"]
